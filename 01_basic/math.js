let val=43;

// randdom 
let first=40;
let last=50;
console.log(Math.floor(Math.random()*(last-first)+first));
// Math.ceil(),Math.floor(),Math.round(),math.Tofixed() this is used for how many point needs after digit
// 